<ul class="tree">
	<li> <a href="/index.html">首页</a> <li>
	<ul>
		<% 
			var boards = require('../models/boards');
			var subs = boards.allSub();
			for (var i = 0; i < subs.length; i++) { %>
				<a href="/forum.html?bid=<%= subs[i].id %>"><%= subs[i].title %></a> 
		 <% } %>
	</ul>
</ul>